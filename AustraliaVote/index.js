/* Vue plugin config */
Vue.prototype.$http = axios;
Vue.prototype.wx = weui;

/* API */
var get_topic = (topicId, callback) => {

}
new Vue({
    el: '#app',
    data() {
        return {
        }
    },
});